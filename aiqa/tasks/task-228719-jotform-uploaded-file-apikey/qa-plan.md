# qa-plan — 228719

## Scope проверки

- Скачивание файлов JotForm в AMS через `IJotFormClient.DownloadUploadedFileAsync`.
- E2E: заявка с вложениями → байты — изображение/PDF, не HTML → успешная передача в Snap при здоровом Apex.

## Окружение

- Сборка с фиксом (например **1.0.106.175660**).
- Валидный JotForm API key в конфигурации AMS.

## Evidence

- Лог: исходящий GET к uploads, статус **200**, отсутствие подстроки `<!DOCTYPE html` в теле (или бинарный контент).
- Успешное завершение onboarding для сценария с файлами.

## Подход

1. Ручной E2E: foreign + US citizen (как в инциденте), минимум одна заявка с **несколькими** фото (combined path).
2. **Автотесты .NET:** только в **репозитории AMS** (в этом workspace DevReps решения AMS нет — команда не из `aiqa/`). Локально: открыть клон AMS и выполнить `dotnet test` по `.sln` / конкретному `*Tests.csproj`, куда попали `JotFormClient` / `ImageUploadService`. На PR тот же набор уже гоняет **pipeline AMS в Azure DevOps** (как у сборки 175660) — это основной сигнал «тесты прошли», если не гоняешь локально.
3. Регресс: методы `GetSubmissionAsync`, списки submissions — смоук.

## Приоритет автоматизации

- **Сейчас:** тест на HTTP handler / интеграционный проверяющий наличие `APIKEY` в запросе к произвольному Uri.
- **Позже:** полный E2E против реального JotForm (флейки, секреты).

## Связанные задачи

- После успешного скачивания вложений проверьте отсутствие **Atlas 400** по `catAccountholderType`: `aiqa/tasks/task-catAccountholder-atlas-400-default-I/` (TC-CAT-400-04).

## Готовность к релизу

- E2E passed на целевом стенде + зелёный pipeline сборки, содержащей коммит 228719.
