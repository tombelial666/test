---
description: AI QA Framework activation rules for ETNA_TRADER workspace
alwaysApply: true
---

# AIQA Framework — Always-On Rules

## Start every task here

Check `aiqa/QUICK_START.md` for the right skill. Invoke it. Don't derive from scratch.

## Context load order

1. `aiqa/QUICK_START.md`
2. Skill file from `.cursor/skills/`
3. `aiqa/docs/knowledge/framework-current-state.md` (if framework context needed)
4. `aiqa/impact-map.yaml` + `aiqa/repo-index.yaml` (impact/regression only)

## Never read by default

- `aiqa/archive/` — historical, not runtime
- `aiqa/docs/knowledge/AI_QA_Framework_V1_Architecture.md` — planned system, not implemented
- `aiqa/docs/knowledge/IDE_Task_Carrier_Pipeline_V1.md` — pilot design, not running
- `aiqa/docs/references/` — audit logs only

## Output

- Produce the artifact directly — not a description of what you'd put in it
- Save to `tasks/[task-id]/` per the skill's output spec
- Mark unconfirmed paths/selectors `[PSEUDOCODE]`
- List unknowns as `[OPEN QUESTION]` at end

## Critical ground truth

- `repo-index.yaml`: review-grade (3 repos: ETNA_TRADER, ServerlessIntegrations, qa)
- `impact-map.yaml`: validation-backed — **not CI-wired**
- Task Carrier / `.aiqa/tasks/` pipeline: **design-phase only, not implemented**
- AMS: not in canonical index — treat as ordinary engineering analysis
- Two QA roots exist: `qa/` and `ETNA_TRADER/qa/` — identify which before any test work

## Repository layout — do not confuse paths

- **Git root / primary workspace:** this repo is `test/` (path like `D:\RepositoryAIQA\test`). All framework work, commits, and pushes you own here start from that root.
- **`aiqa/` (AIQA framework):** `test/aiqa/` — canonical skills catalog, `generate_skills.py`, `azure_deploy_flow.py`, framework docs. **ADO Q1 / dashboard Python scripts** are still **tracked in git here** under `aiqa/scripts/` in many clones, because `qa/` is recorded as a **submodule gitlink** (mode `160000`) without a working `.gitmodules` / nested repo, so `git add` under `qa/` is rejected until that is repaired.
- **`qa/` under `test/`:** `test/qa/` is the **ETNA_TRADER QA** tree (C#/Python tools, SpecFlow, etc.). **Target layout** for the ADO dashboard package (same as on Azure DevOps) is **`test/qa/Tools/aiqa-dashboard/`** — keep scripts and docs there locally and in the real `qa` repo; skills and docs refer to `qa/Tools/aiqa-dashboard/scripts/` as the canonical **path** once the tree is versioned correctly.
- **Remote `origin` → ADO `qa`:** use **`git fetch` / targeted push** for syncing with the standalone `qa` repo; do **not** create a second full clone at `D:\RepositoryAIQA\qa` beside `test/` unless the user explicitly asks. To version `Tools/aiqa-dashboard` inside ADO `qa`, fix submodule metadata (`.gitmodules` + `git submodule update --init qa`) or commit from a proper `qa` working copy, then bump the gitlink in `test` if you keep the submodule model.
